1. Replace the picture 2 and picture 4 with just more pictures of custom dashboards on a laptop

2. The LHS that has the "Projects", Dashboards, Chat, Organizations, etc. should be collapsible on both mobile and laptop

3. Fix loading times to become faster over the whole website, button clicks, file uploads executives

4. Make the plots be able to be zoomed in and out of, and also be able to be panned around

5. Fix the powerbi plot, very vague and has a lot of errors, should be dynamic and modifiable - from the pie chart distribution to the main trend analysis being diverse

6. Dashboard Chart Editor

  Users can't customize generated charts.
  - Add X/Y axis swapping via dropdown/modal
  - Allow chart type switching (bar → line → scatter)

7. Export Functionality

  No way to share dashboards outside the platform.
  - PDF export of dashboards
  - Image export (PNG/SVG) of individual charts

8. Smart Plotting Logic

  Fix algorithm to strictly ignore non-numeric columns for chart values (use them only as labels).

9. AI Analyst Agent (from roadmap)

  A chatbot that can answer questions about the specific uploaded data - this would be a major differentiator.


10. Summary of data to be rendered based off users choice from the chatbot - I want to use a free and open source model's API not something like OpenAI where I have to pay bills if the site becomes really popular
11. Apply rate limiting + secure all endpoints + strict RBAC 

12. Integrate two payment gateways - stripe for US customers and another one for local like nigerian + crypto payment API

13. Improve SEO