# Project Audit & Gap Analysis

## 1. Visual Design Overhaul
- **Current Status**: "Stale" slideshow exists (simple cross-fade of Unsplash images). Themes (`neon-night`, `corporate-blue`) are present in `globals.css` and switcher.
- **Gap**: Slideshow needs to be "modern, high-quality, professional" with text/overlays and better transitions (slide+fade).
- **Fix**: Replace `HeroSlideshow.tsx` with a more complex `framer-motion` implementation (carousel behavior) and better assets.

## 2. CSV Upload & Plotting
- **Current Status**: 
    - Frontend: `UploadWizard` previews first 100 rows, then uploads raw file to R2.
    - Backend: `api/projects/[projectId]/dashboard` fetches **entire file** into memory (`getFileContent`) and processes it synchronously.
- **Gap**: 
    - "Fix existing bug where CSV files do not load fully". The synchronous full-load approach will fail/timeout on large files (100k+ rows).
    - No streaming or chunked processing.
- **Fix**: Implement robust backend parsing (possibly using `nodejs-polars` lazy frames or streaming) and async processing if needed.

## 3. Real-Time Collaboration
- **Current Status**: `ProjectFeed.tsx` uses **Polling** (SWR, 5s interval).
- **Gap**: Requirement specifies "Real-time (use WebSockets, Firebase, Supabase)". Polling is not true real-time and inefficient.
- **Fix**: Refactor `ProjectFeed` to use Supabase Realtime or a WebSocket server (e.g. `socket.io` with custom server, or Pusher).

## 4. AI-Powered Insights
- **Current Status**: `DashboardView` has UI for insights. Backend `analysis-engine` (pending check) likely has some logic.
- **Gap**: Verify if "Context-aware, accurate" analysis is actually occurring or just simple heuristics.
- **Fix**: Integrate a stronger LLM step or advanced statistical heuristics if current ones are too basic.

## 5. Performance
- **Current Status**: Client-side preview is limited. Backend is memory-heavy.
- **Fix**: Optimization of `generateDashboard` and usage of Workers.

##6. API queries too heavy (KB) and too slow (seconds - im trying to obtain double digit ms): 
tested GET/ https://www.antlyst.xyz/sign-in 
it returned - Status: 200 OK | Size: 12.31 KB | Time: 2.15 s

##7. UI Modifications:

4th picture doesn't load anything. just says "Modern analytics interface"

Whole UI needs to get re-adapted to accomodate for phones

3 Lines should show at the top right of the homepage in a settings dropdown type theme that says "Projects", "Dashboard", "Organizations", "Notifications", "Chat", "Subscription".

Under projects we should have projects all the plots generated by the user where they can filter to get a particular one by name, date (ALSO we should add a delete button so that users can update projects so that the UI isn't too clogged up),
while under dashboards we can see which plots we have generated under the different dashboard styles accessible
then under Organizations, Users can:
 create custom organizations with 2 invites per person for the FREE plan with a max size of 5 people per invite. the invite link should be generated and it shouldn't expire, it should only become invalid if people in that current organization are > 5 and when that happens the person that created the link should be sent an in-site notification under "Notifications" to tell them that a user requested to join their organization but they're over the limit so they should upgrade their plan to allow more users join. 
 join too (with a alphanumeric custom code of 8 characters) along with the invite link that would make them sign up to the site if not signed up and automatically join the organization
 notifications can also be shown when a new user JOINS an organization
under Organizations too we should have also have a link routing us to the chat section of the site
under "Chat" if user doesn't belong to an organziation it should show them they don't belong to an organization. to have the chat feature you must belong to an organization
    if they do belong to an organziation then they can access the organization's chat room - communication should be fast and lightweight. im thinking of using supabase's websocket for this, I already have my supabase key and URL in my .env 
under "Subscription", it would normally just show different subscriptions for this site but I havent thought about the costgs for it yet so Im not going to do anything  abouit it, for now it should just display a "Subscriptions coming soon..." message and also enforce the faxct that this project is still in development


